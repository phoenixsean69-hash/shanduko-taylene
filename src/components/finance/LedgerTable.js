import {money} from '../../data/appData.js';export function LedgerTable({kind,title,subtitle,data}){const total=data.reduce((s,r)=>s+r[4],0);const dev=kind==='development';return `<section class="surface-card ledger-view ${dev?'development-ledger':'admin-ledger'}"><header class="surface-card-header ledger-head"><div><div class="ledger-label">${dev?'MODULE B':'MODULE A'}</div><h2>${title}</h2><p>${subtitle}</p></div><button class="secondary-button"><i class="bi bi-download"></i> Export</button></header><div class="ledger-summary"><div><span>Entries</span><strong>${data.length}</strong></div><div><span>Total</span><strong>${money(total)}</strong></div><div><span>Integrity</span><strong>Verified</strong></div></div><div class="table-scroll"><table class="data-table ledger-table"><thead><tr><th>Receipt # (Paper)</th><th>Processing Date</th><th>Member Association</th><th>Bank Branch Context</th><th>Amount (USD)</th><th>Status</th></tr></thead><tbody>${data.map(r=>`<tr><td><code>${r[0]}</code></td><td>${r[1]}</td><td><strong>${r[2]}</strong></td><td>${r[3]}</td><td class="amount">${money(r[4])}</td><td><span class="status-badge">Verified</span></td></tr>`).join('')}</tbody></table></div></section>`}
